# local Claude Code settings — not part of the deliverable
.claude/

# runtime output: verify/deploy JSONL logs and drift-runner logs
*.jsonl
logs/
ves-verify/

# generated baselines and manifests (captured per host, never source)
baselines/
*.manifest.json

# scratch: rendered page images, document-review output, pre-change snapshots.
# Working material, never part of the deliverable. Unanchored so a scratch dir
# at any depth is covered.
tmp/

# editor / OS noise
*.swp
*~
Thumbs.db
