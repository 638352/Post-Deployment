# local Claude Code settings — not part of the deliverable
.claude/

# runtime output: verify/deploy JSONL logs and drift-runner logs
*.jsonl
logs/
ves-verify/

# generated baselines and manifests (captured per host, never source)
baselines/
*.manifest.json

# scratch: rendered page images, document-review output, pre-change snapshots.
# Working material, never part of the deliverable. Unanchored so a scratch dir
# at any depth is covered.
tmp/

# linked git worktrees checked out inside this repo. Committed once as a bare
# 160000 gitlink with no .gitmodules, which git cannot populate: a fresh clone got
# an empty directory and `git archive` shipped a phantom entry. Ignored BEFORE it
# was untracked, so `git status` does not immediately offer it back.
Post-Deployment-datadog-558667ed/

# editor / OS noise
*.swp
*~
Thumbs.db
