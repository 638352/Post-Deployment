# This folder is retired (2026-08-05)

The nested checkout that lived here was fast-forwarded into the parent repo
(`C:\Users\howardr01\Post-Deployment`, commit cc6c12e) and its contents removed.
The empty folder remains only because running shells held its directory handle;
delete it after a reboot. Do not recreate a checkout here.
